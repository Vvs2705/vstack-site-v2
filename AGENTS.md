<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes - APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# VSTACK Site - Agent Handoff

Before continuing work, read:

```txt
docs/CURRENT_PROJECT_STATUS.md
```

Current project direction:

- Do not use Builder.io.
- Do not add new features before stabilizing the visual foundation.
- Continue the frontend refactor through design tokens, primitives, layout consistency and responsive QA.
- `npm run build` currently passes after the latest visual foundation work.
- `npm run lint` still has older issues and should be handled as a separate cleanup phase.

